
  # Fix UI Responsive Layout

  This is a code bundle for Fix UI Responsive Layout. The original project is available at https://www.figma.com/design/scUknmIjVCLWw21JtTB82o/Fix-UI-Responsive-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  